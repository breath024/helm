# 2026-08-13

> 이 로그는 2026-08-14에 `HANDOFF_CHAT.md`에서 옮겨온 것이다(내용은 그때 쓴 그대로).
> 당시엔 앱 이름이 **ClaudeDock**이었다.

## 무엇을 했나
채팅형 프로토타입(`chat_proto.py` + `static_chat/`)을 호윤이 실제로 써보고 올린 증상을
그대로 재현해서 원인까지 확인하고 고쳤다. 백업은 `_backup/20260813/`.

## 터진 것 / 고친 것
| 증상 | 진짜 원인 | 고친 방법 |
|---|---|---|
| CLI 창이 켜졌다 꺼졌다 하며 포커스를 뺏음 | `.lnk`가 **pythonw**(콘솔 없음)로 띄우는데 `claude.exe`는 콘솔앱이라, 윈도우가 새 콘솔(윈도우 터미널)을 만들어 띄운다 | `anyio.open_process`를 감싸 **CREATE_NO_WINDOW** 주입 (SDK엔 이 플래그를 넘길 통로가 없다) |
| 다른 대화를 누르면 쓰던 게 꺼짐 | 세션이 **하나뿐**이라 `start()`가 기존 걸 먼저 끊었다 | 세션 매니저(`SESSIONS`, 최대 6개). 대화마다 따로 살아있고 화면만 갈아탄다 |
| 오류: `No conversation found with session ID` | ① 대화가 적어둔 **cwd를 못 찾으면 홈으로 대체**해서 CLI가 세션을 못 찾음 ② 제목 레코드만 있는 **껍데기 jsonl**이 목록에 있었음 | `resolve_cwd()`(같은 폴더 형제 세션 → 프로젝트 경로 역대조), 껍데기는 목록에서 제외, 실패 메시지 한국어로 |
| 사용량이 이상하게 나옴 | ① `total_cost_usd`는 턴별이 아니라 **세션 누적**인데 프론트가 계속 더해서 2턴째부터 부풀었다 ② rate limit을 `__dict__` 통째로 넘겨 JSON이 안 되니 **문자열**이 됐고, 화면에서 `Object.entries`가 그걸 **한 글자씩** 항목으로 찍었다 | 비용은 서버가 계산(리셋 감지 포함), 결과줄에 `이번/누적/토큰`. 한도는 필드만 뽑아 `한도 62% 씀 · 08:20 초기화` |

곁다리로 같이 고친 것: 연결 실패한 클라이언트를 남겨 **좀비 세션**(살아있는 척하고 전부
`Not connected`)이 되던 것, 소켓 재연결 시 대화가 두 번 그려지던 것(과거/실시간 칸 분리).

**출력이 이상하던 것 (처음부터 있던 버그)**: 흘려 쓰던 말풍선을 완성본으로 갈아끼울 때
`streaming` 클래스를 안 뗐다. 그러면 답변 끝에 커서 `▌`가 계속 깜빡이고, 그 클래스의
`white-space:pre-wrap`이 마크다운 위에 남아 **목록·줄바꿈이 벌어진 채로** 보인다.
갈아끼울 때 `className`을 되돌리고, 빈 채로 남은 말풍선은 지우는 `finishLive()`를 뒀다
(툴 호출·생각·턴 종료 시점에 호출). 확인: 남은 streaming 0개, `white-space: normal`,
목록이 `<li>`로 렌더, 툴카드 = 입력 pre + 결과 pre 2칸.

## 헛짚은 것
- ~~콘솔 창은 안 생긴다~~ → **틀린 확인이었다.** `--server`로 콘솔에서 띄워 점검해서 안 보였을
  뿐이고, `.lnk`(pythonw)로 띄우면 창이 뜬다. 점검 방식이 실사용과 달라서 못 본 것.
  실측하려면 창 클래스 `CASCADIA_HOSTING_WINDOW_CLASS`/`ConsoleWindowClass`를 100ms 간격으로 훑을 것.

## 확인한 것
세션 3개 동시 생존·전환·개별 닫기, 새로고침 후 복원, 소켓 강제 끊고 재연결 시 줄 수 그대로
(19→19), Haiku 2턴 실제 호출로 누적비용 검산(0.0271 + 0.0033 = 0.0304).
남은 잡음: 세션을 닫는 순간 브라우저 콘솔에 `Invalid frame header` 한 줄 — 소켓 정리 과정의
잡음이고 동작엔 영향 없음(평소 사용 중엔 0건). 본체 app.py의 winpty 방식은 창 안 뜬다(실측).

## 미결로 남긴 것
"본체를 채팅형으로 갈아끼울까?" — 호윤이 "공식으로 갈아끼워도 되겠는데?"라고 물었고,
**아직 이르다**고 답해둔 상태. 갈아끼우기 전에 채워야 할 것: 탭·분할, 코드 편집기,
400개 넘는 긴 대화, 코드 하이라이팅. 그리고 **실사용 시간이 없다**(만든 당일).
결론: 둘 다 두고 며칠 채팅형을 주력으로 써본 뒤 호윤이 판단.
