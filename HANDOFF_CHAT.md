# 채팅형 프로토타입 — 핸드오프

`chat_proto.py` + `static_chat/`. **HELM 본체(app.py)와 완전히 별개**로 돌아간다.
(2026-08-14 개명: ClaudeDock → HELM. 폴더명만 그대로 — 로고·아이콘은 `HANDOFF.md` 머리말 참고.)
본체는 하나도 안 건드렸다. 본체 문서는 `HANDOFF.md`.

## 다음에 해야 할 것
- **본체를 이걸로 갈아끼울까 (미결)** — 호윤이 "공식으로 갈아끼워도 되겠는데?"라고 물었고
  **아직 이르다**고 답해둔 상태. 채워야 할 것: 탭·분할, 코드 편집기, 400개 넘는 긴 대화,
  코드 하이라이팅. 그리고 실사용 시간이 부족하다. 둘 다 두고 며칠 써본 뒤 호윤이 판단.
- 이 창을 실제로 띄워 **아이콘 확인** (2026-08-14에 본체만 실측했다)
- 대화 삭제·이름변경, 본문 검색 (지금 검색은 제목만)

## 기록
- [2026-08-14](logs/2026-08-14-log.md) — 개명 ClaudeDock → HELM, 로고·아이콘
- [2026-08-13](logs/2026-08-13-log.md) — 실사용 버그 4개(창 번쩍임·세션 전환·세션ID 오류·비용 부풀림)

## 실행
`HELM 채팅.lnk` 더블클릭. 점검은 `python chat_proto.py --server 8796`.

## 이게 뭐냐
**Claude Code를 Claude 앱처럼** 쓰는 창. 왼쪽에 지난 대화 목록(제목은 claude가 붙인
`ai-title`), 클릭하면 그 대화가 말풍선으로 되살아나고, 거기서 이어서 말할 수 있다.
터미널에서 하던 대화도 그대로 열린다 — 같은 세션 파일을 읽으니까.

## 본체와 뭐가 다른가
| | 본체 (app.py) | 이 프로토타입 |
|---|---|---|
| claude 실행 | ConPTY로 CLI를 물고 xterm이 그림 | **Claude Agent SDK**(`claude-agent-sdk`) |
| 화면 | 터미널 그대로 | 말풍선 · 툴카드 · 권한 모달 |
| 권한 확인창 | 불가(CLI에 플래그 없음) | **가능** — `options.can_use_tool` 콜백 |
| 세션 수 | 여러 개 탭/분할 | 최대 6개 (사이드바에서 전환·개별 닫기) |
| 지난 대화 | 목록만, 열면 터미널 | **말풍선으로 되살림 + 이어서 대화** |

## 검증한 것 (실제 호출, Haiku, 총 ~$0.10)
- 세션 시작 → 질문 → 툴 호출 → 답변까지 왕복
- **권한 확인창이 실제로 뜸** (Write, Bash)
- **거절이 실제로 먹힘** — 파일이 만들어지지 않았고, 거절 사유가 claude에게 전달돼
  claude가 그걸 읽고 다른 방법을 제안함
- UI 렌더: 말풍선/생각/툴카드/배지/모달/입력창 자동높이, JS 에러 0건

## 지난 대화 읽기 (`history()`)
`~/.claude/projects/*/*.jsonl`을 파싱해 UI 이벤트로 바꾼다. 블록 종류가 UI와 1:1로 맞다
(text / thinking / tool_use / tool_result).
- **제목은 `ai-title` 레코드** — claude가 자동으로 붙여둔 것. 없으면 첫 발화로 대체
- **끼워넣은 메시지 거르기**: 스킬·훅이 넣은 user 레코드엔 `isMeta: true` 또는
  `sourceToolUseID`가 붙는다. 진짜 사람이 친 건 `origin.kind == "human"`.
  안 거르면 "Base directory for this skill:" 같은 게 사용자 말풍선으로 뜬다(실제로 겪음)
- 400개 넘는 대화는 뒤쪽만 렌더(`MAX_RENDER`). 5MB짜리 세션 파일도 있음
- **툴카드·생각은 기본 접힘** — 안 접으면 툴카드 136개가 화면을 도배한다.
  접힌 줄엔 파일명 요약(`peek`)만 보여줌

## 읽기 편하게 만든 것
- **마크다운 렌더** (`marked` + `DOMPurify`, `static_chat/vendor/`에 자체 포함).
  안 하면 `**굵게**`·백틱이 글자 그대로 보여서 답변이 번잡해진다 — 호윤이 지적한 지점.
  DOMPurify를 반드시 통과시킬 것(모델 출력에 HTML이 섞일 수 있음, XSS 차단 검증함).
  사용자 말풍선은 `textContent` 그대로 — 사람이 친 건 마크다운이 아니다.
- **작동중 배너** — 입력창 위에 스피너 + "읽는 중 · app.py" + 경과 시간.
  툴 이름은 `TOOL_VERB`로 한국어 동사에 매핑, 파일명은 `peek()` 재사용.
- 툴카드·생각 접기(위 참고)

## 스트리밍 · 백그라운드
- **글자 단위 타이핑**: `include_partial_messages=True` → `StreamEvent`의
  `content_block_delta.delta.text`만 뽑아 `{t:"delta"}`로 흘린다. 완성본
  `AssistantMessage`가 뒤이어 오므로, 화면에서 흘리던 말풍선을 **마크다운 완성본으로 교체**한다.
  ⚠ delta는 `log`에 쌓지 말 것 — 재연결 때 조각이 다시 흘러 중복된다.
- **작업표시줄 깜빡임**(`flash_taskbar`): 턴이 끝났을 때 다른 창을 보고 있으면
  `FlashWindowEx`로 알린다. **포커스는 뺏지 않는다** — 뺏으면 하던 일이 끊긴다.
- ~~콘솔 창은 안 생긴다~~ → **틀렸던 확인이다.** 그때는 `--server`로 콘솔에서 띄워
  점검해서 안 보였을 뿐이고, `.lnk`(pythonw)로 띄우면 창이 뜬다. 지금은 위의
  `CREATE_NO_WINDOW`로 막아둔 상태. 실측 스크립트는 창 클래스
  `CASCADIA_HOSTING_WINDOW_CLASS`/`ConsoleWindowClass`를 100ms 간격으로 훑으면 된다.

## 권한 모델 (실측)
`can_use_tool`은 **모든 툴 호출마다 오는 게 아니라, CLI가 물어봐야 한다고 판단한 것만** 온다.
- 읽기 전용(`Read` 내부 경로, `ls` 같은 Bash)은 확인 없이 통과
- **쓰기는 확인함** — `Write`, 그리고 `touch` 같은 쓰기성 Bash도 확인함
- cwd 밖 경로를 읽으려 할 때도 확인함

즉 "위험한 것만 물어본다"가 기본 동작이고, 그래서 확인창이 덜 귀찮다.
전부 다 물어보게 하려면 `disallowed_tools`나 훅을 따로 써야 한다(미검증).

## 함정
1. **웹소켓 재연결 시 백로그가 다시 온다.** `subscribe()`가 이미 답한 권한요청과
   순간 상태(busy/ratelimit)를 걸러낸다. 안 걸러내면 **답이 끝난 권한창이 다시 뜬다**
   (실제로 겪음). 백로그 이벤트엔 `replay: True`가 붙으니 비용 중복집계도 이걸로 막는다.
2. **API 키를 환경에서 지운다** (`child_env`) — 안 지우면 구독이 아니라 크레딧으로 과금될 수 있음.
   본체와 같은 이유로 자식 세션 표식도 지운다.
3. **비용이 실제로 든다.** 본체(터미널)와 달리 이건 켜두고 질문할 때마다 구독 사용량을 쓴다.
   상단에 누적 비용을 표시해둔 이유. `total_cost_usd`는 **턴별이 아니라 세션 누적**이니
   더하지 말 것(더하면 부풀어 오른다). 압축(compact) 때 0으로 돌아가는 것만 따로 처리한다.
4. **세션마다 기록이 따로다.** 웹소켓은 `?key=` 로 붙고, 재연결하면 서버가 그 세션 기록을
   처음부터 다시 보낸다 → 프론트는 **실시간 칸만 비우고** 다시 그린다(과거 칸은 유지).
5. **세션은 동시에 최대 6개**(`MAX_SESSIONS`). claude 프로세스가 그만큼 뜨니 무한정 열지 말 것.
6. **콘솔 없는 부모(pythonw)에서 콘솔앱을 띄우면 창이 뜬다.** claude를 새로 띄우는 코드를
   건드릴 땐 `CREATE_NO_WINDOW`가 유지되는지 꼭 확인할 것.

## 안 넣은 것
- **계정 자동 전환**: 한도 초과 시 다른 계정으로 바꾸는 기능은 약관 위반이라 안 만듦.
  대신 `RateLimitEvent`를 화면에 표시하고, `fallback_model`로 한도 시 Haiku로 자동 강등.
- 탭·분할 화면 (세션은 여러 개 살아있지만 화면은 한 번에 하나)
- 대화 삭제·이름변경, 검색은 제목만 (본문 검색 아님)
